version: "3.8"

services:
  app:
    build: .
    container_name: clothing_store_api
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    restart: always
